export type MatchConfrontationCanvasProps = {
  height: number;
  leftAccent: string;
  leftWinner: boolean;
  reduceMotion: boolean;
  rightAccent: string;
  rightWinner: boolean;
  width: number;
};
