# Legacy Clutch Web

This directory contains the historical Clutch web application. Active product
development now happens in `../mobile`.

Keep this code as a reference while the native migration is in progress, but
do not add mobile logic here. Before porting anything from `web/`, verify that
an equivalent native feature does not already exist under `mobile/src/features`.
