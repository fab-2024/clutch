import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function MatchesScreen() {
  return (
    <PlaceholderScreen
      eyebrow="ARENA"
      title="Matchs"
      description="Le prochain chantier : calendrier, fiche match et pronostic tactile natif."
    />
  );
}
